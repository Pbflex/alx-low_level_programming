#include "main.h"

/**
 * _abs - main function
 *
 * @n: use to store value
 * _putchar - use to print characters
 * Return: always 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
